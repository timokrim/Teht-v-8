/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { savepipe } from './save.pipe';

describe('savepipe', () => {
  it('create an instance', () => {
    let pipe = new savepipe();
    expect(pipe).toBeTruthy();
  });
});
