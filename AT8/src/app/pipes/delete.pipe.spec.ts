/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { deletepipe } from './delete.pipe';

describe('deletepipe', () => {
  it('create an instance', () => {
    let pipe = new deletepipe();
    expect(pipe).toBeTruthy();
  });
});
